

sub shift_rows {
    my ($self, $in) = @_;
    my $ret;

    for my $row (@$in) {
        @$row =.
    }

    return $ret;
}

sub rot_word {
    my ($self, @in) = @_;
    my @ret;

    for my $i (1..$#in) {
        push @ret, $in[$i];
    }
    push @ret, $in[0];

    return @ret;
}

